<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luca.mapper.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.luca.pojo.entity.User">
        <id column="id" property="id" />
        <result column="avatar_id" property="avatarId" />
        <result column="email" property="email" />
        <result column="enabled" property="enabled" />
        <result column="password" property="password" />
        <result column="username" property="username" />
        <result column="dept_id" property="deptId" />
        <result column="phone" property="phone" />
        <result column="job_id" property="jobId" />
        <result column="create_time" property="createTime" />
        <result column="last_password_reset_time" property="lastPasswordResetTime" />
        <result column="nick_name" property="nickName" />
        <result column="sex" property="sex" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, avatar_id, email, enabled, password, username, dept_id, phone, job_id, create_time, last_password_reset_time, nick_name, sex
    </sql>

</mapper>
